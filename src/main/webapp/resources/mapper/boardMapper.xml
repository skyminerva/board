<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- DAO Name Space --> 
<mapper namespace="com.naemom.board.dao.BoardMapper">

	<!-- 게시판 전체 조회 -->
	<select id="getListBoard" resultType="com.naemom.board.dao.vo.BoardVo">
		SELECT boardId, title, userName, birth, gender, createDate, updateDate
		FROM naemom;
		
	</select>
</mapper>
	